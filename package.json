{
    "name": "webdnn-ssd",
    "scripts": {
        "build": "browserify app.js -o bundle.js -t [ babelify --presets es2015 ]"
    },
    "devDependencies": {
        "babel-core": "^6.26.0",
        "babel-preset-es2015": "^6.24.1",
        "babelify": "^8.0.0",
        "browserify": "^16.1.0"
    },
    "dependencies": {
        "webdnn": "^1.2.2"
    }
}
